name: Suspend SSAS Test

on:
  workflow_dispatch:  

jobs:
  Deploy:
    name: Suspend SSAS 
    uses: ./.github/workflows/Template_tasks_test.yml
    with:
      DeploySSMS: false
      DeployADF: false
      DeploySSAS: false
      SuspendSSAS: true
    secrets: inherit

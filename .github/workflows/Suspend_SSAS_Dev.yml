name: Suspend SSAS Dev

on:
  workflow_dispatch:  

jobs:
  Deploy:
    name: Suspend SSAS 
    uses: ./.github/workflows/Template_tasks_dev.yml
    with:
      DeploySSMS: false
      DeployADF: false
      DeploySSAS: false
      SuspendSSAS: true
    secrets: inherit
